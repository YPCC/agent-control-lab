# Configuration

Primary file: `config/config.yaml`. Override with `AGT_CONFIG_PATH`.

Providers: xai | openai | google | mock via `llm.provider` or `AGT_LLM_PROVIDER`.
Langfuse: `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, `LANGFUSE_HOST`.
